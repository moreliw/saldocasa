// Schema mínimo para Fase 0. Modelos completos (users, households, transactions etc.) entram na Fase 1.
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
